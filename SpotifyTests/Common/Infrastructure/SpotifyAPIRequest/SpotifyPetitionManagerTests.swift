//
//  SpotifyPetitionManagerTests.swift
//  SpotifyTests
//
//  Created by Natalia Goyes on 26/07/22.
//

import XCTest
@testable import gndjf


/*
class SpotifyPetitionManagerTests: XCTestCase {
    
    private var sut: SpotifyPetitionManager!
    
    private var restClient: RESTClient!
    
    private var fakeSpotifyURLBuilder: SpotifyURLBuilder!
    
    private var fakeSpotifyURLRequestBuilder: SpotifyURLRequestBuilder!
    
    private var fakeKeychainFetchManager: KeychainFetchManager!
    
    override func setUp() {
        
        super.setUp()
        
        restClient = RESTClient()
        
        fakeSpotifyURLBuilder = SpotifyURLBuilder()
        
        fakeKeychainFetchManager = KeychainFetchManager()
        
        fakeSpotifyURLRequestBuilder = SpotifyURLRequestBuilder(keychainFetchManager: fakeKeychainFetchManager)
        
        sut = SpotifyPetitionManager(RESTClient: restClient, spotifyURLRequestBuilder: fakeSpotifyURLRequestBuilder, spotifyURLBuilder: fakeSpotifyURLBuilder)
        
    }
    
    override func tearDown() {
        sut = nil
        super.tearDown()
    }
    
    func test_WHEN_makeRequestIsCalled_GIVEN_validParameters_THEN_itShouldSendRequest(){
        let validSearch = "a valid string"
        let validQuery = QueryType.artist
        
        sut.makeRequest(for: validSearch, query: validQuery) { <#Data#> in
            <#code#>
        } onError: { _ in
            XCTFail()
        }
        
    }
}
*/
